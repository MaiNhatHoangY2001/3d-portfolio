name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Adjust as per your main branch name
permissions:
  contents: write
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install dependencies 🔧
        run: |
          npm install -g @angular/cli pnpm
          pnpm install

      - name: Build Angular project 🔧
        run: ng build --configuration production --base-href /3d-portfolio/

      - name: Deploy to GitHub Pages 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages
          folder: dist/3d-showcase  # Update with your Angular project folder
